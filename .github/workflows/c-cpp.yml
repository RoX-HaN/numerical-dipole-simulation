name: C++ Build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4   # 🔥 updated version

      - name: Compile
        run: g++ *.cpp -o program

      - name: Run
        run: ./program
